CREATE TABLE TRAJETHORAIRE (ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, DATECIR DATE NOT NULL, NUMTR INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE TRONCON (ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, TDARR TIME NOT NULL, TDDEP TIME NOT NULL, PRIX FLOAT NOT NULL, GARRIV BIGINT, GARDEP BIGINT, PRIMARY KEY (ID))
CREATE TABLE GARE (ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, GCODE INTEGER NOT NULL, GLAT FLOAT NOT NULL, GLONG FLOAT NOT NULL, GNOM VARCHAR(255) NOT NULL UNIQUE, GVILLE VARCHAR(255) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE UTILISATEUR (ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, ISAD SMALLINT DEFAULT 0 NOT NULL, ADRE VARCHAR(255) NOT NULL, LOGIN VARCHAR(255) NOT NULL, MDP VARCHAR(255) NOT NULL, NAISS DATE NOT NULL, NOM VARCHAR(255) NOT NULL, PRENOM VARCHAR(255) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE TRAJETHORAIRE_TRONCON (TrajetHoraire_ID BIGINT NOT NULL, trajet_ID BIGINT NOT NULL, PRIMARY KEY (TrajetHoraire_ID, trajet_ID))
ALTER TABLE TRONCON ADD CONSTRAINT FK_TRONCON_GARRIV FOREIGN KEY (GARRIV) REFERENCES GARE (ID)
ALTER TABLE TRONCON ADD CONSTRAINT FK_TRONCON_GARDEP FOREIGN KEY (GARDEP) REFERENCES GARE (ID)
ALTER TABLE TRAJETHORAIRE_TRONCON ADD CONSTRAINT TRJTHRRTRNCONtrjtD FOREIGN KEY (trajet_ID) REFERENCES TRONCON (ID)
ALTER TABLE TRAJETHORAIRE_TRONCON ADD CONSTRAINT TRJTHRRTRNTrjtHrrD FOREIGN KEY (TrajetHoraire_ID) REFERENCES TRAJETHORAIRE (ID)
